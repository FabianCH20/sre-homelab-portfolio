# prometheus.yml
global:
  scrape_interval: 15s
rule_files:
  - "alert.rules.yml"
scrape_configs:
  - job_name: "node"
    static_configs:
      - targets: ["<IP_SERVIDOR>:9100"]
  - job_name: "kubernetes"
    static_configs:
      - targets: ["<IP_SERVIDOR>:10250"]
